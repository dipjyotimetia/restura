info:
  type: http
  name: Get user
  seq: 1
http:
  method: GET
  url: "{{API_BASE}}/users/1"
  headers:
    - name: Accept
      value: application/json
runtime:
  scripts:
    - type: tests
      code: |
        pm.test('status is 200', () => pm.response.to.have.status(200));
