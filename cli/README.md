# @restura/cli

Run Restura API collections in CI.

## Install

```bash
npm install -g @restura/cli
```

## Quick start

```bash
restura run ./api-tests --reporter junit --output junit.xml
```

See `docs/cli/README.md` for full docs (added in a later task).
