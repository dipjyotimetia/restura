# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
.next/
out/
dist/
build/

# Coverage / test reports
coverage/
playwright-report/
test-results/

# Electron
electron/dist/

# Package manager
package-lock.json
yarn.lock
pnpm-lock.yaml

# Generated files
*.generated.*
.next/types/**/*.ts
src/lib/opencollection/spec-types.ts
docs/CAPABILITY_MATRIX.md

# Vendored third-party schemas — keep upstream formatting, never rewrite
vendor/

# Test fixtures — formatting is intentional (some are asserted on byte-for-byte)
tests/fixtures/
cli/fixtures/
extension/vscode/test/fixtures/

# Vendored OpenCollection JSON Schema copied into the VS Code extension — keep
# byte-identical to vendor/opencollection (do not reformat).
extension/vscode/schemas/
# Downloaded VS Code + test profile (created by @vscode/test-cli).
extension/vscode/.vscode-test/

# MDX (docs-site content): Prettier's MDX printer prose-wraps Markdown lists and
# code fences nested inside JSX components (<TabItem>, <FileTree>, <Aside>), which
# corrupts them. `astro check` validates these files in the docs CI job instead.
*.mdx

# Cache / tool dirs
.cache/
.turbo/
.wrangler/
.husky/_/
*.tsbuildinfo

# Claude Code session worktrees / local agent scratch (git-ignored, machine-local)
.claude/worktrees/

# Generated by `npm run echo:local`
echo-local/manifest.json
echo-local/restura-echo-local.collection.json
