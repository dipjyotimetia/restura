{
  "name": "@restura/extension",
  "version": "1.6.2",
  "private": true,
  "type": "module",
  "description": "Restura browser capture extension — capture multi-protocol traffic into Restura collections.",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "type-check": "tsc --noEmit -p tsconfig.json",
    "gen:store-assets": "node scripts/gen-store-assets.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2"
  }
}
